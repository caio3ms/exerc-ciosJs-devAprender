// Criar uma função que exibe a quantidade de * da linha

function exibitAsteriscos(n) {
    let ast = '';
    for (let i = 1; i <= n; i++) {
        ast += '*'
        console.log(ast)
    }
}